# Job (One-Time Task)
apiVersion: batch/v1
kind: Job
metadata:
name: myjob
spec:
template:
spec:
containers:
- name: myjob
image: busybox
command: ["echo", "Hello from Job!"]
restartPolicy: Never
backoffLimit: 3